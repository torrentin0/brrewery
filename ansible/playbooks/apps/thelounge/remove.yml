---
- hosts: localhost
  connection: local
  become: true
  gather_facts: true
  roles:
    - brrewery_user
  tasks:
    - name: Look up brrewery user home directory
      ansible.builtin.getent:
        database: passwd
        key: "{{ brrewery_user }}"
      register: brrewery_passwd

    - name: Set brrewery home directory fact
      ansible.builtin.set_fact:
        brrewery_home: "{{ brrewery_passwd.ansible_facts.getent_passwd[brrewery_user][4] }}"

    - name: Stop and disable thelounge for brrewery user
      ansible.builtin.systemd:
        name: "thelounge@{{ brrewery_user }}.service"
        enabled: false
        state: stopped
        daemon_reload: true
      failed_when: false

    - name: Remove nginx reverse proxy for The Lounge
      ansible.builtin.include_role:
        name: brrewery_nginx_site
        tasks_from: remove
      vars:
        brrewery_app_id: thelounge

    - name: Remove thelounge user config and data
      ansible.builtin.file:
        path: "{{ brrewery_home }}/.thelounge"
        state: absent

    - name: List remaining thelounge user service unit files
      ansible.builtin.shell: |
        set -o pipefail
        systemctl list-unit-files 'thelounge@*.service' --no-legend --no-pager 2>/dev/null || true
      register: thelounge_unit_files
      changed_when: false

    - name: Remove thelounge systemd template when no instances remain
      ansible.builtin.file:
        path: /etc/systemd/system/thelounge@.service
        state: absent
      when: thelounge_unit_files.stdout_lines | length == 0
      notify: Reload systemd

    - name: Remove thelounge package when no instances remain
      ansible.builtin.apt:
        name: thelounge
        state: absent
      when: thelounge_unit_files.stdout_lines | length == 0

  handlers:
    - name: Reload systemd
      ansible.builtin.systemd:
        daemon_reload: true
